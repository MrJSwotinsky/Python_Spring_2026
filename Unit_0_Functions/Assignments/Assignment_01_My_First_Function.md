## Unit 0, Assignment 1 - My First Function
Due: Monday, February 2nd 2026

### Create a Function that Mathematically Manipulates a Number

1.  In your edube.org sandbox, use comments to describe a series of at least three mathematical manipulations you can apply to a number.

    *For example,* 
    ```python
    # Double the number.
    # Subtract 5 from the result.
    # Square the result.
    ```

2.  On paper, apply your mathematical manipulations to at least three different numbers.  **Show All Work.**

    *For example,*<br><img src =  https://github.com/MrJSwotinsky/Python_Spring_2026/blob/main/Resources/Number_Manipulations.png>

3.  Write a function named `math_manipulator` that takes a number, `x`, as an argument, and returns the result after the series of manipulations you described above have been applied to the number.

     *For example,*

     ```python
     def math_manipulator(x):
         x = 2 * x
         x = x - 5
         x = x ** 2
         return x
     ```

4.  Call your function with each of the numbers you selected in step 2, and display the result.
      *For example,*

     ```python
     print(math_manipulator(2))
     print(math_manipulator(5))
     print(math_manipulator(-1.5))
     ```

5.  Compare the results of your program to your results on paper.  Troubleshoot and resolve any discrepancies.

6. Create a folder for assignments in your portfolio titled `LastNameFirstInitial_Assignments`.
  
7. Create a file in your assignments folder titled `LastNameFirstInitial_My_First_Function.py`
8. Copy/Paste your code from the sandbox to `LastNameFirstInitial_My_First_Function.py`.
9. Commit your work.

     **Remember to include an appropriate commit message**.
 
10.  Capture an image of your work from step 2, save it as `LastNameFirstInitial_My_First_Function.py`. and upload it to your assignments folder.
11.  Commit your work.
    
     **Remember to include an appropriate commit message**.  
