## Unit 0, Assignment 2 - My Second Function
Due: Monday, February 2nd 2026

### Write a Function that Accepts Two or More Arguments and Displays a Sequence of Actions

1.  In your edube.org canvas, write a function that accepts two or more arguments and displays a description of a sequence of actions.

    *For example,* 
    ```python
    def workout(workout_1, workout_2, workout_3, athlete, n):
        print(athlete + ' completes ' + n + ' workout_1.')
        print(athlete + ' completes ' + n + ' workout_2.')
        print(athlete + ' takes a 60 second break.')
        print(athlete + ' completes ' + n + ' workout_3.' )
    ```

2.  Using comments, explain what you expect when your functions is called:

    *For example,* 
    ```python
    # I expect my function to display the workout.
    # For example, if I call workout with the arguments, 'pushups', 'pullups', 'squats', 'Ada', and 3...
    # I expect the console to display...
    # Ada completes 3 pushups.
    # Ada completes 3 pullups.
    # Ada takes a 60 second break.
    # Ada completes 3 squats.
    ```
   
3.  Call your function.  Remember to pass an argument to every parameter.

    *For example,* 
    ```python
    workout('pushups', 'pullups', 'squats', 'Ada', 3)
    ```

4.  Run your program.  Did it perform as expected?  If not, review and revise your code as needed, and try again.

5.  Write two additional function calls by passing different arguments to your function.
6.  Run your program.  Did it perform as expected?  If not, review and revise your code as needed, and try again.
7.  Create a file in your assignments folder titled `LastNameFirstInitial_My_Second_Function.py`
8.  Copy/Paste your code from the sandbox to `LastNameFirstInitial_My_Second_Function.py`.
9.  Commit your work.

     **Remember to include an appropriate commit message**.
