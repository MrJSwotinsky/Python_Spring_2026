## Unit 0, Assignment 3 - Arguments and Parameters
Due: Wednesday, February 4th 2026

### Write and Call a Function

1.  In your edube.org canvas, write a simple function that meets the following requirements:
      * Your function has exactly three parameters.
      * Two of your parameters do **NOT** have default values.
      * One of your parameters **DOES** have a default value.

2.  Call your function using only the **positional** argument passing technique.  

3.  Comment out your previous function call, and call your function using only the **positional** argument passing technique.  **This time, change the order of the arguments.**

4.  Comment out your previous function call, and call your function using only the **keyword** argument passing technique.  **Pass the arguments in the same order as the parameters.**

5.  Comment out your previous function call, and call your function using only the **keyword** argument passing technique.  **This time, change the order of the arguments.**

6.  Comment out your previous function call, and call your function using a combination of the **positional** and **keyword** argument passing techniques.  **Pass all positional arguments before all keyword arguments.**  
   
7.  Comment out your previous function call, and call your function using a combination of the **positional** and **keyword** argument passing techniques.  **This time, pass at least one keyword argument before at least one positional argument.**

8.  Comment out your previous function call, and call your function using exactly two positional arguments.
   
9.  Comment out your previous function call, and call your function using exactly four positional arguments.  

### Reflect on your Learning and Exploration

1.  Create a file in your assignments folder titled `LastNameFirstInitial_Parameters_and_Arguments.md`

2.  Copy/Paste the code block below to your file.
  
3.  Copy/Paste the code for your function and function calls to the `My Code` section of your file, between the tickmarks.

4.  Respond to each prompt. 

5.  Commit your work.

      **Remember to include an appropriate commit message**.

````markdown
## Parameters and Arguments

# My Code
```python

```
## Reflection

Respond to the following Based on what you learned in class as well as your work exploring arguments and parameters...

1. Describe the positional argument passing technique.
2. Describe the keyword argument passing technique.
3. If you use the keyword argument passing technique, but arrange arguments in a different order than the parameters they are passed to, which arguments will be passed to which parameters?
4. If you use the positional and keyword argument passing techniques in a single function call, what must you always remember to do?  What will happen if you don't do this?
5. Explain the role of a parameter's default value?
6. How can you determine the number of arguments a function call must have?
7. Explain what a default value is and what it does?
8. What happens if you call a function with too few arguments (assuming no parameters have default values)?
9. What happens if you call a function with too few arguments (assuming some parameters have default values)?
10. What happens if you call a function with too many arguments?
````
